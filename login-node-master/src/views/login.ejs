<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Login</title>
    <% include partials/head %>
  </head>
  <style>
    .s{
      background:white;
    }
  </style>
  <body class="login">
  <% include partials/navigation %>
    <div class="container">
      <div class="col-sm-6 mx-auto">
        <div class="card mt-5">
          <div class="card-body">
            <!-- messages -->
            <h1 class="text-center">
              <span class="fa fa-sign-in"></span> Ingresar
            </h1>
            <% if (message.length > 0) { %>
              <div class="alert alert-danger">
                <%= message %>
              </div>
            <% } %>

            <!-- form -->
            <form action="/login" method="post">
              <div class="form-group">
                <label for="email">Correo</label>
                <input type="email" name="email" class="form-control s">
              </div>
              <div class="form-group">
                <label for="password">Clave</label>
                <input type="password" name="password" class="form-control s">
              </div>
              <input type="submit" class="btn btn-dark btn-lg">
            </form>

            <hr>
            <div class="text-center">
              <p>¿Necesitas una cuenta? <a href="/signup">Registro</a></p>
              <p>Ir a <a href="/">inicio</a>.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
